table ip filter {  
    chain forward {  
        type filter hook forward priority 0; policy drop;  
  
        ip saddr 10.0.0.1 ip daddr 20.0.0.1 tcp dport 80 accept  
        ip saddr 10.0.0.1 ip daddr 20.0.0.1 tcp dport 443 accept  
    }  
}